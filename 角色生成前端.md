# 角色生成前端

本文档用于沉淀 Noema 角色生成前端的最终实现目标：直接对齐 ComfyUI_frontend 的成熟画布工作台，把角色生成部分做成可自由配置的 Agentic RP 资源图编辑器。这里的“不接后端”只表示暂不调用真实 LLM、生图、TTS 或导出服务；前端交互、组件结构、画布能力、连线逻辑和工具化布局必须按最终产品标准一次到位，不允许做 MVP、临时版、简化版或占位式兼容实现。

## 0. 不可降级原则

本项目不接受“先做一个能用的第一版，再逐步补齐”的实现路线。角色生成前端必须按 ComfyUI_frontend 的工作台级成熟度直接实现。

硬性原则：

- 不做静态流程图。节点、slot、link、panel、tab、toolbar 都必须来自真实前端状态。
- 不做装饰性连线。连线必须能拖拽、连接、断开、重连、校验兼容性并参与序列化。
- 不做简化画布。必须支持 pan、zoom、fit view、选择、多选、拖拽、resize、z-index、折叠、上下文菜单和浮层工具。
- 不做临时三栏页面。最终结构必须是全屏画布 + 可折叠/可 resize 面板 + 浮层工具栏。
- 不做“以后再接”的 UI 空壳。可以不调用后端，但前端状态、事件、交互和 mock 数据必须完整闭环。
- 不以当前 Noema 旧实现为上限。以 ComfyUI_frontend 的 graph workspace 为对齐对象。
- 不回避成熟 TypeScript 库。如果 LiteGraph、Fuse、Yjs 或其他 TS 库能更快达到成熟体验，可以直接引入；是否引入的判断标准是最终交互质量，而不是维持当前文件简单。
- 不为了兼容旧的固定 workflow 模型而牺牲资源图语义。旧 `createStandardCharacterWorkflow()` 只能作为迁移参考，不能继续主导前端架构。

## 1. 当前判断

Noema 现有 chat 角色资源已经能被加载和使用：`role/chat/<id>/manifest.json` 描述角色，图片资源由主进程读成 data URL 后交给 renderer，聊天时会把角色描述、故事、背景、首句、标签、场景状态和摘要作为上下文传给 chat 模型。

当前的角色工作流前端已经有画布雏形、节点、侧栏、inspector、文件 tab 和运行按钮，但它更像一个固定流程展示器：

- 节点来自 `createStandardCharacterWorkflow()`，流程被预设为 brief -> 角色卡 -> 视觉规格 -> 图片提示 -> 图片资源 -> 校验 -> 导出。
- 连线目前是装饰性的 flow line，不是真正的数据连接。
- slot 只有视觉展示，没有拖线、类型兼容、断开、重连、从空白处创建节点等交互。
- inspector 已经能编辑参数，但参数、节点状态、资源预览和图结构还没有形成完整的前端图编辑模型。
- 运行按钮暗示“执行固定工作流”，这不符合后续 Agent 自主生成资源、前端只表达资源配置和约束边界的目标。

目标不是把前端做成一个固定 LLM pipeline，而是做成 Agentic RP 资源配置系统：前端表达资源、约束、依赖、偏好和验收边界，后端 Agent 在这些边界内自主规划、生成、修订和打包。

## 2. ComfyUI_frontend 是怎么做的

参考目录：`other/ComfyUI_frontend`。

ComfyUI_frontend 是 Vue + Vite + TypeScript 项目，核心图编辑体验建立在 LiteGraph 之上，并叠加 Vue 组件、PrimeVue/Reka UI、Pinia store、Yjs layout store、Fuse 搜索和自定义 design system。

### 2.1 页面结构

Comfy 的 graph 页面是“全屏画布 + 工具浮层”：

- 底层是 `GraphCanvas`，负责 LiteGraph canvas 初始化和图渲染。
- 画布上方叠加 `LiteGraphCanvasSplitterOverlay`，提供 workflow tabs、side toolbar、sidebar、right side panel、bottom panel、top menu 等区域。
- Builder 模式下额外叠加 `BuilderToolbar`、`BuilderMenu`、`BuilderFooterToolbar`。
- 右下角有 `GraphCanvasMenu`，提供 fit view、zoom、小地图、隐藏/显示连线等画布操作。

重要设计点：

- 画布是第一层，所有工具 UI 都是可开关、可折叠、可 resize 的浮层。
- sidebar 和 right panel 不是写死在页面里，而是通过 splitter 和 slot 组合。
- 工具栏不占据业务内容主导权，始终服务画布。

### 2.2 节点系统

Comfy 的 Vue 节点渲染不是一个大组件，而是清晰拆分：

- `LGraphNode`：节点外壳，负责位置、选中、拖动、resize、折叠、透明度、执行态、错误态、右键菜单。
- `NodeHeader`：标题、折叠按钮、状态和 header 交互。
- `NodeSlots`：统一渲染输入输出 slot。
- `InputSlot` / `OutputSlot`：slot 标签、tooltip、连接点、兼容状态、拖线入口。
- `SlotConnectionDot`：不同数据类型和 shape 的连接点视觉。
- `NodeWidgets`：节点参数控件。
- `NodeContent`：图片、视频、预览、媒体内容。
- `NodeFooter`：子图、错误入口、advanced inputs 等辅助操作。

这个拆法值得直接对齐。Noema 的角色资源节点也应该拆为：

- 节点壳：负责画布交互。
- 节点头：资源类型、标题、状态、折叠。
- slot：资源输入输出。
- 参数区：资源配置字段。
- 内容区：角色卡、图像、语音、世界观、场景或规则预览。
- footer：校验、打开详情、复制、导出等操作。

### 2.3 连线系统

Comfy 的连线系统成熟点在于它把 DOM 节点、slot 测量、连接器和绘制解耦：

- DOM slot 负责交互和视觉。
- `useSlotElementTracking` 测量 slot 元素位置，并写入 layout store。
- slot 初次测量后缓存相对 node 的 offset，节点移动时不反复读 DOM。
- `slotLinkDragUIState` 维护当前拖线状态、pointer 位置、source slot、候选目标和兼容 map。
- `linkConnectorAdapter` 包装 LiteGraph 的连接器，提供 beginFromInput、beginFromOutput、dropOnCanvas、disconnectMovingLinks、兼容判断等 API。
- `linkDropOrchestrator` 根据 pointer target 解析 slot candidate 或 node surface candidate。
- `LinkOverlayCanvas` 是独立 canvas overlay，负责连线绘制，不让 DOM 节点承担绘线。

关键能力：

- 从 input 或 output 都能开始拖线。
- 支持移动已有连接、断开连接、批量断开。
- 拖线时实时 dim 不兼容 slot。
- hover 到节点表面时可以自动推断第一个兼容 slot。
- drop 到空白处时可以触发搜索/菜单，创建可连接节点。
- 连线绘制支持曲线、直线、箭头、流动动画、隐藏连接等模式。

Noema 需要对齐的是这套交互完整度，而不是必须完全复制 LiteGraph 内部实现。

### 2.4 节点库和搜索

Comfy 有两种节点添加入口：

- 常驻 Node Library sidebar：用于浏览、分组、排序、搜索、收藏、预览、查看帮助。
- 画布 Node Search dialog：用于快速添加节点，支持分类、过滤、键盘选择和 hover preview。

搜索服务基于 Fuse，支持：

- 按名称、显示名、别名搜索。
- 按 input type 过滤。
- 按 output type 过滤。
- 按 category 过滤。
- 按 source 过滤。
- 从一根正在拖的线释放到空白处时，自动按当前连接类型过滤可承接节点。

Noema 应该将其改造成 Resource Library：

- 角色基础资源。
- 世界观资源。
- 场景资源。
- 对话风格资源。
- 视觉资源。
- 语音资源。
- 记忆/长期状态规则。
- RP 约束。
- 安全/版权/一致性校验。
- 导出/打包节点。
- Agent 策略和运行边界节点。

### 2.5 Builder 模式

Comfy 的 Builder 模式不是执行工作流，而是让用户把 graph 转成 app：

- 顶部步骤：选择输入、选择输出、排列布局。
- 左上菜单：保存、进入 app mode、退出 builder。
- 底部工具条：上一步、下一步、保存、另存为、预览 app。

这对 Noema 的启发是：角色生成前端也应该有“构建资源包”的模式，而不是只有“运行工作流”。

Noema 可对应为：

- 选择核心角色资源。
- 选择可暴露给聊天的运行时状态。
- 排列资源预览和测试面板。
- 保存资源图。
- 预览角色包。
- 进入聊天测试。
- 导出角色资源。

## 3. ComfyUI_frontend 做到了什么程度

Comfy 已经是一个成熟的生产级 graph editor 前端，做到的程度包括：

- 完整画布生命周期：初始化、加载 workflow、恢复 tabs、自动保存、drop 文件、copy/paste、快捷键。
- 完整 layout store：节点、连线、slot、reroute、z-index、选择、resize、拖动。
- 完整节点交互：选择、拖动、alt 复制拖动、折叠、resize、右键菜单、执行态、错误态。
- 完整 slot/连线交互：类型兼容、拖线、重连、断开、空白 drop 创建节点、reroute。
- 完整面板体系：sidebar、right inspector、bottom panel、top menu、workflow tabs、floating canvas toolbar。
- 完整节点库：搜索、过滤、分类、收藏、帮助、预览、拖拽。
- 完整 Builder/App mode：同一张 graph 可从编辑模式转为 app 使用模式。
- 完整主题和视觉系统：design-system、palette、slot color、node color、dark/light 适配。

换句话说，Comfy 的前端不是“画几个节点”，而是一套图形化工作台。

Noema 的目标应直接对齐这个工作台级别，但语义不是图像生成 pipeline，而是角色生成/角色资源/Agentic RP 配置。

## 4. Noema 前端目标

目标：直接完成一个与 ComfyUI graph workspace 同级别的角色资源图工作台。后端生成能力可以暂时不接，但前端必须完整表现最终态：真实 graph state、真实节点库、真实连线、真实 slot 兼容、真实 inspector、真实资源包预览、真实本地序列化和完整工具化布局。

### 4.1 产品定位

这个页面应该是“角色资源生成与约束配置工作台”，不是“运行一个固定工作流”。

前端要表达：

- 用户希望生成什么角色。
- 角色有哪些核心资源。
- 哪些资源相互依赖。
- 哪些资源是可选素材。
- 哪些规则约束 Agent。
- 哪些资源必须一致。
- 哪些内容需要校验。
- 最终打包成什么形式。

后端 Agent 后续根据这些资源图自主执行：规划、调用模型、生成资源、修订、校验、打包。

前端必须已经能完整编辑这些边界。即使后端返回的是 mock 或空实现，用户也必须能像使用 ComfyUI 一样完成资源图的构建、调整、检查和预览。

### 4.2 页面布局

应对齐 Comfy 的工具工作台布局：

- 顶部：角色资源图 tabs、资源包 tabs、运行记录 tabs。
- 左侧：Resource Library，可切换资源、节点库、收藏、模板和帮助。
- 中央：自由画布，支持 pan、zoom、fit view、选择、多选、框选、拖拽、resize、折叠、层级。
- 右侧：Inspector，可编辑节点参数、slot、连接、资源预览、校验状态和 Agent 策略。
- 底部：资源包动作条，包含保存、预览、校验、聊天测试、导出入口。
- 右下：canvas controls，包含缩放、fit view、小地图、隐藏连线、布局模式。
- 浮层：节点搜索、上下文菜单、资源预览、错误/校验面板。

当前固定三栏 grid 不应作为目标形态保留。实现应直接替换为“全屏画布 + splitter 面板 + 浮层工具”的工作台结构。

### 4.3 核心组件

前端必须拆成这些组件或模块，不能继续堆在单个 render 函数里：

- `CharacterResourceWorkspace`
- `CharacterResourceTabs`
- `CharacterResourceSplitterOverlay`
- `CharacterResourceCanvas`
- `CharacterCanvasViewport`
- `CharacterCanvasControls`
- `CharacterMiniMap`
- `CharacterSelectionRectangle`
- `CharacterNode`
- `CharacterNodeHeader`
- `CharacterNodeSlots`
- `CharacterInputSlot`
- `CharacterOutputSlot`
- `CharacterSlotDot`
- `CharacterNodeWidgets`
- `CharacterNodeContent`
- `CharacterNodeFooter`
- `CharacterLinkOverlay`
- `CharacterPathRenderer`
- `CharacterLinkDragState`
- `CharacterLayoutStore`
- `CharacterSlotLayoutTracker`
- `CharacterNodeLibrary`
- `CharacterNodeSearchPopover`
- `CharacterNodePreviewCard`
- `CharacterInspector`
- `CharacterAssetPanel`
- `CharacterValidationPanel`
- `CharacterBottomToolbar`
- `CharacterContextMenu`
- `CharacterGraphSerializer`
- `CharacterGraphCommandRegistry`

这些组件必须全部服务真实前端状态。后端未接入时可以使用 mock resource output，但不能用 mock 交互代替真实交互。

## 5. 角色资源图语义

Noema 不应该照搬 `llm / image / validation / export` 分类。应该按 Agentic RP 角色资源分类。

### 5.1 节点类别

建议第一批节点类别：

- `brief`：角色需求、目标用户、互动目标、禁区。
- `identity`：姓名、称呼、年龄段、身份、职业、标签。
- `persona`：性格、价值观、缺陷、目标、边界、秘密。
- `world`：世界观、时代、阵营、地点、规则。
- `scene`：初始地点、状态变量、装备、关系、场景规则。
- `dialogue`：首句、说话风格、称呼规则、示例对话。
- `visual`：外观设定、发型、眼睛、服装、色板、负面特征。
- `image`：头像、立绘、表情包、服装、场景参考。
- `voice`：音色、语气、TTS 配置、示例台词。
- `memory`：长期记忆策略、总结策略、关系变量、状态更新规则。
- `rp-rule`：角色扮演约束、叙事边界、沉浸规则。
- `safety`：版权、年龄、安全、风格边界。
- `critic`：一致性检查、资源完整性检查、冲突检查。
- `export`：角色包 manifest、图片资源、聊天资源目录、预览包。
- `agent-policy`：后端 Agent 的自主程度、可改写范围、重试/修订策略、工具权限。

### 5.2 Slot 类型

slot 类型决定依赖和兼容性。建议第一批：

- `character-brief`
- `identity-card`
- `persona-card`
- `world-context`
- `scene-state`
- `dialogue-style`
- `visual-spec`
- `image-prompt`
- `image-asset`
- `voice-profile`
- `memory-policy`
- `rp-constraint`
- `safety-policy`
- `validation-report`
- `character-pack`
- `agent-policy`

连接线表示资源依赖、约束作用域或打包引用，不表示固定执行顺序。

### 5.3 连接语义

需要支持多种连接语义：

- `requires`：目标节点依赖源资源。
- `constrains`：规则节点约束目标节点。
- `references`：目标节点引用源素材。
- `validates`：校验节点检查目标资源。
- `exports`：导出节点收集目标资源。
- `suggests`：弱依赖，只作为 Agent 参考。

连接 UI 必须能表达 link kind。可以默认创建 `requires`，但用户应能在 inspector 或连接菜单中切换连接语义；不同语义至少要有可区分的样式、标签或 tooltip。

## 6. 库和架构取舍

用户的判断是对的：Comfy 用的很多库本质都是 TypeScript 生态，Noema 当前 Electron + Vite 架构可以兼容。是否引入库的判断标准不是“维持当前代码简单”，而是能否更快达到 Comfy 级别的最终体验。

### 6.1 允许直接引入

- `litegraph.js` 或 Comfy fork 中可迁移的 LiteGraph 模块：必须作为 graph 底座优先采用；除非能证明自研实现达到同等交互完整度和维护性，否则不能手写玩具版 graph editor。
- `fuse.js`：必须用于节点/资源搜索，支持名称、别名、分类、输入/输出类型过滤。
- `yjs`：如果 layout store、undo/redo 或未来协作需要 CRDT 语义，可以直接使用，不以后置为默认。
- canvas2D path renderer：可从 Comfy 的 `CanvasPathRenderer` 思路迁移，必须支持曲线、直线、箭头、中心 marker、flow animation 和 hit detection。
- splitter / floating UI / menu 类库：只要与当前 Electron renderer 兼容，可以引入。不能因为当前没有 Vue/PrimeVue 就降低面板交互标准。
- icon 库：必须用真实图标表达工具按钮，不能用文本按钮或临时符号替代。

### 6.2 不允许以架构现状为理由降级

- 不能因为当前 renderer 是 TypeScript 字符串模板，就继续把 graph workspace 写成单文件模板。
- 不能因为没有现成 splitter，就保留固定三栏。
- 不能因为没有现成 graph 库，就做静态节点和装饰线。
- 不能因为后端未接入，就省略资源包预览、校验面板、运行记录 tab、mock 输出状态和错误状态。
- 不能因为现有 SDK workflow 是固定节点，就把前端自由图限制成固定流程。

### 6.3 最终架构要求

- 图状态：nodes、links、groups、tabs、selection、viewport、panel layout、history、mock run outputs 都必须可序列化。
- layout：必须维护 node bounds、slot layout、link geometry、z-index、collapsed/expanded size、panel sizes。
- 交互：必须拥有 command registry，支撑快捷键、菜单、上下文菜单和按钮调用同一批命令。
- 渲染：节点 DOM 与 link overlay 解耦；slot 位置由 layout tracker 同步，不允许每帧粗暴读 DOM。
- 搜索：节点库和拖线释放搜索必须共用同一个 search service。
- 主题：视觉必须贴合现有 Noema chat，但交互密度、面板结构和节点工作台成熟度对齐 Comfy。
- mock 后端：所有后端动作必须有前端 mock lifecycle，如 queued、running、done、failed、canceled、stale。

## 7. 最终验收标准

前端完成后必须达到以下标准，不能以“先实现一部分”视为完成。

### 7.1 画布

- 支持 pan、zoom、fit view、reset view。
- 支持小地图或等价的 graph overview。
- 支持选择、多选、框选、取消选择。
- 支持节点拖动、对齐、z-index 前置、alt/duplicate 或菜单复制。
- 支持节点 resize、折叠、展开。
- 支持空白画布右键菜单。
- 支持键盘快捷键入口。

### 7.2 节点

- 节点由真实 definition 渲染，不由固定流程硬编码。
- 节点包含 header、slot、widgets、content、footer。
- 节点支持状态：idle、dirty、queued、running、done、failed、stale、disabled。
- 节点支持错误态、警告态、选中态、执行态。
- 节点支持资源预览：文本卡、图像占位、语音占位、规则摘要、校验摘要、导出包摘要。
- 节点参数控件支持 text、textarea、number、integer、boolean、select、multi-select、string-list，并有错误/辅助状态。

### 7.3 Slot 和连线

- input/output slot 必须有类型、标签、tooltip、必填状态和连接状态。
- 支持从 input 或 output 开始拖线。
- 支持连接、断开、移动已有连接、重连。
- 支持类型兼容校验。
- 拖线时不兼容 slot 必须降噪或禁用，兼容 slot 必须高亮。
- 支持 drop 到节点表面并自动推断兼容 slot。
- 支持 drop 到空白处打开节点搜索，并只显示可连接节点。
- 支持 link kind：requires、constrains、references、validates、exports、suggests。
- link overlay 必须使用真实 geometry 绘制，不允许装饰性线条。
- 连线必须参与序列化和恢复。

### 7.4 节点库和搜索

- 左侧 Resource Library 必须支持分类、搜索、过滤、收藏/常用、预览、帮助。
- 节点搜索弹窗必须支持键盘导航、hover preview、分类筛选、输入/输出类型过滤。
- 从拖线触发搜索时，搜索上下文必须携带 source slot 类型。
- 资源节点和约束节点都必须可从库中添加。
- 节点添加后必须支持 ghost placement 或等价的跟随鼠标放置体验。

### 7.5 面板和工具条

- 顶部必须有资源图/预览包/运行草稿 tab。
- 左侧面板必须可折叠、可 resize。
- 右侧 inspector 必须可折叠、可 resize。
- 底部工具条必须包含保存、预览、校验、聊天测试、导出入口。
- 右下 canvas controls 必须包含 zoom、fit view、隐藏/显示连线、小地图入口。
- 浮层和菜单必须有明确层级，不遮挡核心交互。

### 7.6 状态和序列化

- 当前资源图必须能序列化为前端 JSON。
- 刷新或重开页面后必须能恢复 mock graph。
- panel size、active tab、viewport、selection 至少要有合理恢复策略。
- mock run 输出、mock validation report、mock package preview 必须能从图状态生成。
- 文档 TODO 必须随实现进度勾选，不能只在最终一次性补齐。

### 7.7 明确不属于本轮后端范围

这些后端能力不属于前端完成度，但前端必须预留入口和 mock lifecycle：

- 不调用真实 LLM。
- 不调用真实生图。
- 不调用真实 TTS。
- 不写入 `role/chat/<id>`。
- 不做真实导出 zip 或目录。
- 不执行真实 Agent 规划。

## 8. 后续与后端 Agent 的关系

前端资源图不是后端执行计划。它是 Agent 的输入边界。

后端 Agent 后续应该读取：

- 资源节点定义。
- 节点参数。
- 节点之间的依赖和约束。
- 哪些资源是必须生成的。
- 哪些资源是参考素材。
- 哪些规则不可违反。
- 哪些字段需要用户确认。
- 哪些校验必须通过。

Agent 可以自主决定：

- 调用哪些模型。
- 先生成哪些资源。
- 如何补全缺失信息。
- 如何根据校验结果修订。
- 如何复用已有素材。
- 如何组织最终角色包。

因此，前端不要把“执行步骤”写死成 LLM pipeline。前端应该提供资源、约束和可视化编辑能力；后端负责规划和执行。

## 9. 对当前代码的改造方向

当前 `chat-character-workflow-page.ts` 不能继续作为固定 workflow renderer 扩写。它应该被替换为最终 graph workspace 架构：

- 删除前端对 `createStandardCharacterWorkflow()` 的渲染依赖。SDK workflow 可以保留给后端或迁移参考，但不能主导 UI。
- 新建角色资源图前端 model：node definitions、graph nodes、links、tabs、viewport、panels、selection、mock outputs。
- 将当前单文件 HTML renderer 拆成工作台、画布、节点、slot、连线、库、搜索、inspector、工具条等模块。
- 将装饰性 `.chat-workflow-flow-line` 移除，替换为真实 link overlay。
- 将固定 sidebar 替换为 Resource Library + Assets + Validation + Help 的可切换面板。
- 将固定 inspector 替换为基于 node definition 的属性编辑器。
- 将 `workflow/run/character` tab 替换为 `resource graph / package preview / run draft` 的 tab 模型。
- 将 `run workflow` 操作替换为前端 mock lifecycle：preview、validate、package、chat test。
- 引入或实现成熟 graph 依赖，优先保证交互 parity，不为保留旧结构做兼容层。

最终这个页面应该成为 Noema 的角色资源工作台，而不是 SDK workflow 的可视化调试页。

## 10. 实施 TODO

TODO 是最终实现清单，不是 MVP 清单。实现时每完成一项必须立即勾选，并保留未完成项，不能到最后一次性勾选。

### 10.1 架构和状态

- [x] 建立 `CharacterResourceGraph` 前端数据模型，包含 nodes、links、groups、tabs、viewport、selection、panels、mock outputs。
- [x] 建立 `CharacterResourceNodeDefinition`，覆盖节点类别、参数 schema、input/output slot、默认尺寸、资源预览类型。
- [x] 建立 `CharacterResourceLink`，包含 source、target、slot、kind、label、状态和序列化字段。
- [x] 建立 graph serializer/deserializer，支持保存和恢复完整前端图状态。
- [x] 建立 command registry，统一菜单、按钮、快捷键对 graph 操作的调用。
- [x] 移除 renderer 对固定 `createStandardCharacterWorkflow()` 的直接依赖。
- [x] 将现有 `chat-character-workflow-page.ts` 拆分为多个前端模块，避免继续扩写单个模板文件。

### 10.2 工作台布局

- [x] 实现全屏画布工作台 shell，取代固定三栏 grid。
- [x] 实现顶部资源图 tabs，支持 resource graph、package preview、run draft。
- [x] 实现左侧 Resource Library 面板，支持折叠和 resize。
- [x] 实现右侧 Inspector 面板，支持折叠和 resize。
- [x] 实现底部资源包工具条，包含保存、预览、校验、聊天测试、导出入口。
- [x] 实现右下 canvas controls，包含 zoom、fit view、隐藏连线、小地图入口。
- [x] 实现浮层层级规范，保证搜索、菜单、预览、tooltip 不遮挡核心拖拽。

### 10.3 画布能力

- [x] 实现 pan、zoom、fit view、reset view。
- [x] 实现 viewport 状态序列化和恢复。
- [x] 实现节点选择、取消选择、多选、框选。
- [x] 实现节点拖拽、z-index 前置、复制和删除。
- [x] 实现节点 resize、折叠、展开。
- [x] 实现空白画布右键菜单。
- [x] 实现小地图或等价 graph overview。

### 10.4 节点渲染

- [x] 实现 `CharacterNode` 外壳：位置、尺寸、选中、状态、错误、拖动、resize。
- [x] 实现 `CharacterNodeHeader`：标题、类型、折叠、状态和菜单入口。
- [x] 实现 `CharacterNodeSlots`、`CharacterInputSlot`、`CharacterOutputSlot`、`CharacterSlotDot`。
- [x] 实现 `CharacterNodeWidgets`，覆盖 text、textarea、number、integer、boolean、select、multi-select、string-list。
- [x] 实现 `CharacterNodeContent`，覆盖文本卡、图像占位、语音占位、规则摘要、校验摘要、导出包摘要。
- [x] 实现 `CharacterNodeFooter`，包含错误、详情、预览、连接摘要等入口。
- [x] 实现节点状态视觉：idle、dirty、queued、running、done、failed、stale、disabled。

### 10.5 Slot 和连线

- [x] 建立 slot layout tracker，测量 DOM slot 并缓存相对 node offset。
- [x] 建立 link overlay，使用真实 geometry 绘制曲线/直线/箭头/中心 marker。
- [x] 实现 link path renderer，支持不同 link kind 的样式。
- [x] 实现 slot drag state，记录 source、pointer、candidate、compatible map。
- [ ] 支持从 input 和 output 双向开始拖线。
- [ ] 支持连接、断开、移动已有连接、重连。
- [x] 支持类型兼容校验和不兼容 slot 降噪。
- [ ] 支持 drop 到节点表面自动推断兼容 slot。
- [ ] 支持 drop 到空白处打开节点搜索，并自动过滤可连接节点。
- [x] 支持 link kind 编辑：requires、constrains、references、validates、exports、suggests。
- [x] 将连线纳入 graph 序列化和恢复。

### 10.6 节点库和搜索

- [x] 实现 Resource Library 分类：brief、identity、persona、world、scene、dialogue、visual、image、voice、memory、rp-rule、safety、critic、export、agent-policy。
- [x] 实现节点搜索服务，支持名称、显示名、别名、分类、source、input type、output type。
- [x] 实现 Resource Library 搜索、过滤、排序、收藏/常用。
- [x] 实现节点帮助/说明和 hover preview。
- [x] 实现画布节点搜索弹窗，支持键盘导航、hover preview、分类筛选。
- [ ] 实现从拖线触发搜索时的类型上下文过滤。
- [ ] 实现 ghost placement 或等价的跟随鼠标放置体验。

### 10.7 Inspector 和资源预览

- [x] Inspector 根据 node definition 渲染参数，不写死节点类型 UI。
- [x] Inspector 支持节点标题、描述、状态、尺寸、slot、参数、mock output。
- [x] Inspector 支持 link 选择和 link kind 编辑。
- [x] 实现 package preview tab，从 graph state 生成 mock manifest、资源列表和缺失项。
- [x] 实现 validation panel，从 graph state 生成 mock 校验报告。
- [x] 实现 run draft tab，展示 mock queued/running/done/failed lifecycle。
- [x] 实现聊天测试入口的前端占位 flow，但不调用真实聊天。

### 10.8 视觉和交互对齐

- [ ] 节点视觉密度、slot 尺寸、hover/active/selected 状态对齐 Comfy 工作台成熟度。
- [x] 工具按钮使用图标和 tooltip，不使用临时文本符号替代。
- [ ] 面板 resize、折叠、滚动条、空状态、错误状态都要完整设计。
- [ ] 连接线 hover、选中、隐藏、流动状态都有明确视觉。
- [ ] 搜索弹窗、节点预览、上下文菜单具备完整键盘和鼠标交互。
- [x] 移动端或窄窗口至少不破坏布局；不允许文本和控件重叠。

### 10.9 验证

- [x] 新增前端完成度检查脚本或命令，机械统计上述关键能力是否存在。
- [x] 每完成一个 TODO 后运行完成度检查并更新本文档勾选。
- [x] 每次涉及 desktop renderer 代码后运行 `pnpm --filter @noema/desktop build`。
- [x] 遵守 AGENTS 要求，不启动 Electron、Vite 或浏览器 smoke test，除非用户另行明确要求。
- [x] 保持 `autoresearch-results/` 不提交，文档和代码改动单独可审查。
